{
  "extends": "./tsconfig",
  "compilerOptions": {
    "declaration": false,
    "sourceMap": false,
    "typeRoots": ["node_modules","src/type/*"]
  },
  "include": [
    // Ensure we only bundle production sources
    "src/**/*"
  ]
}
