{
  "extends": "./tsconfig",
  "compilerOptions": {
    "declarationDir": "build/types",
    "declaration": false,
    "sourceMap": false
  },
  "include": [
    // Ensure we only bundle production sources
    "src/**/*"
  ]
}
